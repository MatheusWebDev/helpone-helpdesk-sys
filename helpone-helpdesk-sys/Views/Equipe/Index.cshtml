@model IEnumerable<helpone_helpdesk_sys.Models.Equipe>

@{
    ViewBag.Title = "Index";
}

<h2 class="mt-4">Gerenciar Equipes</h2>

@*<p>
    @Html.ActionLink("Create New", "Create")
</p>*@

<table class="table">
	<tr>
		<th>
			@Html.DisplayNameFor(model => model.Id)
		</th>
		<th>
			@Html.DisplayNameFor(model => model.OMSituada)
		</th>
		<th>
			Qtd. Membros
		</th>
		<th></th>
	</tr>

	@foreach (var item in Model)
	{
		<tr>
			<td>
				@Html.DisplayFor(modelItem => item.Id)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.OMSituada)
			</td>
			<td>
				@Html.DisplayFor(modelItem => item.ListaMembros.Count)
			</td>
			<td>
				@*@Html.ActionLink("Edit", "Edit", new { id = item.Id }) |*@
				@Html.ActionLink("Gerenciar", "Details", new { id = item.Id }) @*|
				@Html.ActionLink("Delete", "Delete", new { id = item.Id })*@
			</td>
		</tr>
	}

</table>
